@using Microsoft.AspNetCore.Identity
@using Trachtenberg_System.Areas.Identity.Data

@inject SignInManager<WebsiteUser> SignInManager
@inject UserManager<WebsiteUser> UserManager

<ul class="navbar-nav">
@if (SignInManager.IsSignedIn(User))
{
    @* <li class="nav-item"> *@
    @*     <a id="manage" class="nav-link text-dark" asp-area="Identity" asp-page="/Account/Manage/Index" title="Manage">Hello @UserManager.GetUserName(User)!</a> *@
    @* </li> *@
    @* <li class="nav-item"> *@
    @*     <form id="logoutForm" class="form-inline" asp-area="Identity" asp-page="/Account/Logout" asp-route-returnUrl="@Url.Action("Index", "Home", new { area = "" })"> *@
    @*         <button id="logout" type="submit" class="nav-link btn btn-link text-dark border-0">Logout</button> *@
    @*     </form> *@
    @* </li> *@
    @* <li> *@
    @*     <a asp-controller="LoggedIn" asp-action="Index"> *@
    @*         <button class="btn glass">Go to dashboard</button> *@
    @*     </a> *@
    @* </li> *@
    <div class="menu menu-horizontal px-1 items-center">
        <li class="nav-item">
            <a id="manage" class="nav-link text-dark" asp-area="Identity" asp-page="/Account/Manage/Index" title="Manage">Hello @UserManager.GetUserName(User)!</a>
        </li>
        <li class="nav-item">
            <form id="logoutForm" class="form-inline" asp-area="Identity" asp-page="/Account/Logout" asp-route-returnUrl="@Url.Action("Index", "Home", new { area = "" })">
                <button id="logout" type="submit" class="nav-link btn btn-link text-dark border-0">Logout</button>
            </form>
        </li>
        <li>
            <a asp-controller="LoggedIn" asp-action="Index">
                Go to dashboard
            </a>
        </li>
    </div>
}
else
{
    @* <li class="nav-item"> *@
    @*     <a class="nav-link text-dark" id="register" asp-area="Identity" asp-page="/Account/Register">Register</a> *@
    @* </li> *@
    @* <li class="nav-item"> *@
    @*     <a class="nav-link text-dark" id="login" asp-area="Identity" asp-page="/Account/Login">Login</a> *@
    @* </li> *@
    <div class="navbar-end flex flex-row gap-4">
        <a asp-area="Identity" asp-page="/Account/Login" class="btn btn-outline">Login</a>
        <a asp-area="Identity" asp-page="/Account/Register" class="btn btn-primary">Register</a>
    </div>
}
</ul>
